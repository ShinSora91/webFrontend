// import axios from "axios";

// export const API_SERVER_HOST = "http://localhost:8080";
// const prefix = `${API_SERVER_HOST}/api/todo`;

// export const getOneData = async (sno) => {
//   const res = await axios.get(`${prefix}/sread/${sno}`);
//   return res.data;
// };

// export const getListData = async (pageParam) => {
//   const { page, size } = pageParam;
//   const res = await axios.get(`${prefix}/slist`, {
//     params: { page: page, size: size },
//   });
//   return res.data;
// };
